# LabWired QA Bug Log — 2026-07-29

**Role:** QA tester (browser + Architect agent)  
**Surfaces:** `labwired.com`, `app.labwired.com` (Playground / Architect), MCP  
**Artifacts:** screenshots + JSON in this folder  
**Browser control:** Playwright headless Chrome (system Chrome). User Chrome tabs also opened for manual login.

---

## Status legend

| Status | Meaning |
|--------|---------|
| **Open** | Reproduced |
| **Open-Review** | Possible product ambiguity |
| **Blocked** | Needs auth / env |
| **Works** | Verified OK this session |

---

## Session setup

| Item | Value |
|------|--------|
| Date | 2026-07-29 |
| Guest | Yes (headless) |
| Signed-in account | **Not yet** — user to login in Chrome |
| LabWired MCP in Grok | Auth required (disconnected) |
| Disk | Was 100% full mid-session; freed ~1.8 GiB temp/caches |

---

## Test plan

| # | Case | Result |
|---|------|--------|
| 1 | Landing loads | Works |
| 2 | Agent product page | Works |
| 3 | Public install one-liner | **FAIL** BUG-001 |
| 4 | Playground lab `esp32c3-oled-lab` | Works (sim running, OLED clock) |
| 5 | Playground lab `nokia5110-invaders-lab` | Works (loads parts/wires) |
| 6 | Validation catalog | Works |
| 7 | Architect starter **LED blink** | Works — **Verified live** as guest |
| 8 | Architect freeform follow-up prompt | **FAIL / confused** BUG-005 |
| 9 | Sign-in modal | Opens (Clerk) when Sign in clicked |
| 10 | MCP unauthenticated | 401 (expected if OAuth-only) BUG-002 |
| 11 | Logged-in private build / Pro agent | **Blocked** — need your login |

---

## Bugs

### BUG-001 — Public install URLs 404 (blocks advertised install)
- **Severity:** **Critical**
- **Status:** Open
- **Surface:** `labwired.com` + Agent page
- **Repro:**
  1. Open https://labwired.com/agent.html
  2. Copy `curl -fsSL https://labwired.com/agent-install.sh | sh`
  3. Or HEAD: `curl -sI https://labwired.com/agent-install.sh` / `/install` / `/agent-install.ps1`
- **Expected:** 200 + install script
- **Actual:** **HTTP 404** (GitHub Pages)
- **Notes:** GitHub source install still 200:  
  `https://raw.githubusercontent.com/LabWired/agent/main/install.sh`  
  Release `v0.1.0` exists but has **zero assets**.

### BUG-002 — Hosted MCP requires auth; Grok `labwired` MCP not connected
- **Severity:** Medium (agent-via-MCP QA blocked)
- **Status:** Open / by-design TBD
- **Surface:** `https://api.labwired.com/mcp`
- **Actual:** `401` + `WWW-Authenticate: Bearer realm="LabWired MCP"`
- **Next:** Complete LabWired OAuth for Grok MCP, retest tools.

### BUG-003 — Community projects “Loading…” (initial concern)
- **Severity:** Low
- **Status:** Open-Review (static crawl showed Loading; not re-confirmed after full JS)
- **Surface:** homepage community section
- **Action:** Re-check live after login/network; may be false positive if API is slow.

### BUG-004 — Free vs Starter $9 pricing messaging
- **Severity:** Low (copy)
- **Status:** Open-Review
- **Surface:** landing + pricing
- **Detail:** Page advertises **Free** Playground + agent harness, and also **Starter $9/mo**. Easy to misread whether Playground is free or $9.

### BUG-005 — Architect freeform send did not post; Feedback modal stole focus
- **Severity:** **High** (blocks “describe a change” path)
- **Status:** Mitigated / retest PASS signed-in (2026-07-29 product-qa; a11y Send vs Feedback #1091). Keep watching guest.
- **Surface:** Playground Architect composer
- **Repro (guest, after LED blink starter):**
  1. Open `https://app.labwired.com/?lab=esp32c3-oled-lab`
  2. Click starter **LED blink** → wait until Architect shows **Verified live**
  3. Type in composer: `Add a second UART print line that says LABWIRED_QA_PROBE every 2 seconds`
  4. Activate send (↑ / Enter / Send)
- **Expected:** Message appears in Architect thread; agent starts work (or clear paywall/sign-in)
- **Actual:**
  - Message **remained** in the composer
  - **Send feedback** modal opened on top (`role=presentation` overlay z-index 1000)
  - UI blocked further clicks (My Projects, etc.)
- **Evidence:** `12-architect-typed.png`, `13-architect-after-send.png`
- **Note:** May be selector/automation misfire on “Send”, but Feedback intercepting is real UX risk if Enter/↑ maps poorly. **Manual retest after login required.**

### BUG-006 — Project title/breadcrumb stale after Architect redesign
- **Severity:** Medium
- **Status:** Open
- **Surface:** Playground chrome
- **Repro:** Load OLED lab → Architect **LED blink** → verified
- **Expected:** Title reflects new design (e.g. LED blink / Super Mini LED)
- **Actual:** Breadcrumb still **“ESP32-C3 0.96" OLED”** while canvas is MCU + red LED only (OLED removed)
- **Evidence:** `11-led-blink-starter.png`

### BUG-007 — Parts search placeholder truncated
- **Severity:** Low (UI polish)
- **Status:** Open
- **Surface:** Parts panel
- **Actual:** Placeholder shows `Search parts, ty` (clipped)
- **Evidence:** all playground screenshots

### BUG-008 — `app.labwired.com/` without `?lab=` still lands on a full lab (OLED)
- **Severity:** Low–Medium (navigation clarity)
- **Status:** Open-Review
- **Detail:** Visiting bare Playground root still showed ESP32-C3 OLED project (not empty home / chooser). May be intentional last-lab restore; confirm empty-state / lab picker for first-time users.

---

## What worked well

1. **OLED lab auto-runs** — SSD1306 clock visible (`19:00:07`), sim **Running**.
2. **Architect guest starter LED blink** end-to-end:
   - Prompt auto-sent: *“ESP32-C3 LED blink…”*
   - Result: **Working device – Verified live on the twin**
   - Canvas rewired to MCU + LED; status **Verified live ✓**
3. **Nokia 5110 lab** loads multi-part board + analyzer UI.
4. **Validation** page lists chip families + 133 components.
5. **No page JS exceptions** in Playwright console for main pass (`session-snapshot.json`).

---

## Build attempts log

| # | Action | Outcome |
|---|--------|---------|
| B1 | Open OLED lab | Sim running, clock UI |
| B2 | Architect starter **LED blink** | **PASS** Verified live (guest) |
| B3 | Freeform UART probe line | **FAIL** — Feedback modal; message not posted (BUG-005) |
| B4 | Account builds / private twins | **Blocked** — sign in |

---

## Screenshots

| File | What |
|------|------|
| `01-landing.png` | Marketing home |
| `02-agent.png` | Agent product page |
| `03-playground-esp32c3.png` | OLED lab |
| `04-after-clicks.png` | After Run/Build/Sign-in |
| `07-playground-home.png` | Bare app URL |
| `07b-nokia.png` | Nokia lab |
| `08-validation.png` | Validation matrix |
| `09-pricing.png` | Pricing |
| `10-agent-ready.png` | Architect starters + running OLED |
| `11-led-blink-starter.png` | **Verified live** LED design |
| `12-architect-typed.png` | Freeform prompt typed |
| `13-architect-after-send.png` | Feedback modal intercept |

JSON: `session-snapshot.json`

---

## Blockers for deeper agent QA

1. **You log in** on Chrome tab: `https://app.labwired.com/` (already focused / open).
2. Optionally enable Chrome: **View → Developer → Allow JavaScript from Apple Events** so I can drive *your* logged-in window via AppleScript.
3. Authenticate **labwired** MCP in Grok (OAuth) for API-side agent testing.

---

## Changelog

- 2026-07-29 session start: public URL probe, bug file created  
- Headless Playwright pass: landing, agent, labs, validation, pricing  
- Architect LED blink verified live as guest  
- Freeform send path hit Feedback modal — BUG-005/006 filed  
- Waiting on user login for signed-in Architect builds  


---

## MCP parity (Claude / OpenAI Codex vs Grok) — 2026-07-29 later

User note: **Claude and OpenAI (Codex) do use these MCP integrations.** Matched their configs into Grok.

| Client | LabWired MCP | Browser control |
|--------|--------------|-----------------|
| **Claude Code** | `https://api.labwired.com/mcp` — **Connected** | (varies) |
| **OpenAI Codex** | LabWired app tools (`labwired_run/lab/verify/compile`) | `@playwright/mcp --extension` + token |
| **Grok (this session)** | Same URL added — **OAuth required / not connected** | Same playwright-browser added — **doctor OK (24 tools)** but tools not yet exposed to this chat until MCP refresh |

### Config written to `~/.grok/config.toml`
- `labwired` → `https://api.labwired.com/mcp`
- `playwright-browser` → `npx @playwright/mcp@latest --extension` + `PLAYWRIGHT_MCP_EXTENSION_TOKEN` (same as Codex)
- `labwired-local` → `npx -y @labwired/mcp` — handshake failed (local stdio); cloud HTTP path preferred

### Actions for user
1. In Grok TUI: open `/mcps`, press **`r`** to reload servers.
2. Authenticate **labwired** OAuth when prompted (Claude already has a working token; Grok needs its own).
3. Ensure Chrome has the **Playwright MCP extension** connected (Codex uses the same token).

### Possible product/doc bug
- Hosted MCP works for Claude OAuth clients, but **Grok session cannot complete handshake without interactive OAuth** — document this next to `claude mcp add` / `codex mcp add` on the landing page if Grok is a first-class client.


### NOTE — Playwright MCP session interruption (2026-07-29 ~16:57)
- Playwright MCP **extension mode** successfully attached to Chrome (same path as Codex).
- Navigating to `app.labwired.com` mid-session hit **Clerk → Google OAuth password challenge** (user was signing in).
- Automated QA script incorrectly targeted a generic `textbox` and interacted with the **Google password field** — **do not automate password fields**.
- Secrets were scrubbed from local QA artifacts.
- **Manual action:** finish Google/LabWired sign-in yourself, then resume Architect QA as guest/signed-in deliberately.


## Signed-in confirmation (2026-07-29 ~17:06)

**YES — user is logged into LabWired Playground.**

| Signal | Value |
|--------|--------|
| URL | `https://app.labwired.com/?lab=esp32c3-oled-lab` |
| Sign in button | **Absent** |
| Open user menu | **Present** (Clerk) |
| Architect | Present (starters + composer) |
| Google password page | Absent |

Evidence: `26-checks.json`, `26-snap.yml`, `27-signed-build.png`

### Follow-up signed-in actions
- LED blink click: **not confirmed** as redesign — still Parts(3) OLED lab; earlier “Verified live” match was false positive from starter copy *“Done means Verified live on the twin.”*
- Freeform send: **Send (↑) stayed disabled**; Meta+Enter did not clear/post message (`27-after-freeform.yml`). Retest BUG-005 carefully with enabled Send after real input event.
- My Projects click: panel state unclear from truncated preview (same OLED shell).

### BUG-009 — Architect Send (↑) remains disabled / freeform hard to submit
- Severity: High
- Status: Open
- After typing into `Message the Architect`, automation still saw `button "Send" [disabled]`. May be fill-vs-input-event issue, or product requires more than empty-state text for enable.


## Multi-chip Architect workflow (2026-07-29 ~17:15–17:33)

**Goal:** Build electronics with different chips via Architect while signed in.

### Labs opened (signed-in ✓)
| Lab | Chip | Parts | Sim |
|-----|------|-------|-----|
| `esp32c3-oled-lab` | ESP32-C3 | 3 | Running |
| `nokia5110-invaders-lab` | STM32L476 | 5 | Running |
| `nrf52840-proximity-lab` | nRF52840 | 4 | Running |
| `rp2040-pico` | RP2040 | 2 | Running |

Session stayed signed in entire time (`Open user menu` present).

### Architect build results
| Action | Chip/Lab | Result |
|--------|----------|--------|
| Starter LED blink | C3 | **FAIL** — no redesign in ~48s (starters remain) |
| Freeform serial probe | C3 | **FAIL** — Send ↑ stayed disabled |
| Starter Serial hello | C3 | **FAIL** — no redesign |
| Freeform STM blinky | Nokia/L476 | **FAIL** — Send disabled |
| Freeform nRF blink | nRF52840 | **FAIL** — Send disabled |
| Freeform RP2040 | Pico | **FAIL** — Send disabled |
| Starter OLED hello | C3 | **FAIL** |
| Starter Prove it works | C3 | **FAIL** |
| Starter Button + LED | C3 | missing once (UI flake) |

Artifacts: `workflow-report.json`, `wf-*.yml/png`

### BUG-011 — Architect starter chips do not run (signed-in session)
- **Severity:** Critical (product agent path)
- **Status:** Open
- **Detail:** Clicking LED blink / Serial hello / OLED hello / Prove it works leaves UI on same lab for 48s+; no “Working device – Verified live” bubble; starter chips remain.
- **Contrast:** Earlier guest headless session once got LED blink → Verified live.

### BUG-009 (confirmed multi-lab) — Architect Send (↑) stays disabled
- Freeform typing (Playwright type/slowly + key events) never enabled ↑ Send across C3, Nokia, nRF, RP2040.
- Meta+Enter does not submit.

### BUG-012 — “Send feedback” button matches Send selectors (a11y/automation trap)
- DOM query for buttons whose label includes “Send” hits **Feedback** (`aria-label: Send feedback`) before Architect ↑.
- Force-click accidentally targets Feedback modal path.
- **Fix:** rename aria to `Feedback` only; Architect send `aria-label="Send message"` without colliding “Send feedback”.

### Diag follow-up
- Native `textarea` value set + `input` event: valueLen=44.
- DOM `.click()` on LED blink: parts count later observed **2** (was 3) once — possible partial redesign; still no verified bubble after 20s (`wf-diag-after.json`).

Report bugs count from run: 5 entries in workflow-report.json


## Fix implementation (2026-07-29) — subagent-driven

Plan: `labwired/docs/superpowers/plans/2026-07-29-qa-bugfix-architecture.md`

| Bug | Status | Where |
|-----|--------|--------|
| BUG-001 install 404 | **Fixed in code** (await deploy) | `landing_page` branch `fix/install-public-scripts` @ 8d9d7a1 |
| BUG-012 Send feedback collision | **Fixed** | monorepo `fix/qa-bugfix-architecture` Feedback → aria Feedback; Architect → Send message |
| BUG-009/011 silent send / busy | **Fixed (UX)** | `architectSendGate` + drop notice when busy; chips still use sendText |
| BUG-006 stale title | **Fixed** | `withAgentOutcomeTitle` after agent apply (label only; cloud names untouched) |

**Not fixed by product code alone:** pure Playwright extension flakiness; `?win32=true` query on GH Pages.

Deploy needed: merge+push landing_page; merge+deploy playground (pages-deploy).


## Live QA workflow (post-deploy) — 2026-07-29T17:02:30.040Z

Report: `docs/qa/LIVE-QA-2026-07-29.json` (128 steps)

### PASS (shipped fixes verified live)
| Check | Result |
|-------|--------|
| Install `/install`, `agent-install.sh`, `install.ps1` | **200** |
| Signed in | **Open user menu** |
| Feedback a11y | **`button "Feedback"`** (no Send feedback) |
| Architect send a11y | **`button "Send message"`** |
| Labs load (C3, Nokia/STM32, nRF, RP2040) | **OK** sim shells |

### FAIL (Architect agent path still broken under automation)
| Case | Result |
|------|--------|
| LED blink / Serial hello / Prove it works starters | timeout ~60s — starters remain, no verified bubble |
| Freeform C3 / Nokia / nRF / RP2040 | Send can enable after InputEvent retype + click, prompt stays, **no agent completion** |

**Bug IDs this run:** ['LIVE-STARTER-TIMEOUT', 'LIVE-FREEFORM-TIMEOUT']

**Interpretation:** Deployed UI a11y + install fixes are good. Architect **backend/turn execution** still does not complete builds when driven via Playwright (chips appear no-op; freeform message may not leave empty-state). Manual click by human recommended to separate product vs automation.

**Next product dig:** Network tab on `/v1/agent` when clicking LED blink; confirm 200 + stream; if no request, React onClick not firing; if 4xx/quota, surface error (busy notice path only covers busy).

---

## Continued QA — 2026-07-29 evening (post deploys)

**Deploys in play:** #1091 a11y, #1092 draft-adopt, #1093/#1094 founder-only reasoning, **#1095 Grok omit hotfix**

### Public smoke

| Check | Result |
|-------|--------|
| `labwired.com/install` + install scripts | **Works** (200) |
| `app.labwired.com` | **Works** (200) |
| Guest `POST /v1/agent` simple chat | **Works** after #1095 (`OK` + finish) |
| Guest LED starter (API curl) | **Works** after #1095 (~6s, hydrate + verified text) |

### Playground a11y (headless guest)

| Check | Result |
|-------|--------|
| `aria-label="Feedback"` | **Works** |
| `aria-label="Send message"` | **Works** |
| Architect starters present | **Works** (LED blink, Serial hello) |
| Freeform does not open Feedback modal | **Works** (BUG-005 fixed for a11y path) |

### Architect builds (headless guest)

| Case | Result | Notes |
|------|--------|-------|
| LED blink | **Works** after #1095 | Verified live ~3–6s; mid-run error before hotfix was regression |
| Freeform UART probe | **Works (transport)** | Sent OK; no Feedback steal; settled ~2m with tool line (firmware edit miss — model quality, not crash) |
| Nokia 5110 lab load | **Works** | Parts + Architect present |

### Bugs found this pass

### BUG-009 — Grok `reasoningEffort: "none"` breaks all Architect streams
- **Severity:** Critical (prod regression)
- **Status:** **Fixed** #1095 (deployed)
- **Cause:** #1093/#1094 set public Grok to `reasoningEffort: 'none'`; xAI rejects that value → SSE `errorText: The agent hit an error mid-run`
- **Fix:** omit Grok reasoning option for public; founder still gets `low`
- **Evidence:** curl LED hydrate then error; after deploy, hydrate + text + finish

### BUG-010 — Pages deploy self-hosted runner EACCES (europa-2)
- **Severity:** Medium (ops) — playground UI deploys flaky
- **Status:** Open
- **Actual:** checkout fails `index.lock` / `.agents/workflows` permission denied
- **Impact:** UI-only changes may lag API; earlier Pages success then repeated failures

### Prior bugs status refresh

| ID | Status |
|----|--------|
| BUG-001 install 404 | **Fixed** (200 live) |
| BUG-005 Feedback steals Send | **Fixed** (a11y Send message / Feedback) |
| BUG-006 stale chrome title | Fixed in #1091 (partial; signed-in title still depends on Pages) |
| BUG-002 MCP auth | Open / by design |

### Artifacts
- `LIVE-QA-CONTINUE-2026-07-29.json`, `qa2-*.png`, `qa2-agent-hits.json`, `qa2-curl-led.txt`


### BUG-011 — edit_firmware oldString miss + target undefined (freeform after LED)
- **Severity:** High (freeform adaptation path)
- **Status:** **Fixed** #1096 (API deployed)
- **Actual:** `firmware → undefined: edits[0].oldString not found`
- **Fix:** compact/WS fuzzy match, always return `target`, `sourcePreview` on miss, single-MCU target recovery


---

## Signed-in QA — 2026-07-29 (user session)

**Account:** shylenkoa@gmail.com (`user_3DqakRYYx8RmlNMggznGdcRYcfd`)  
**Artifact:** `LIVE-QA-SIGNED-IN-2026-07-29.json`, `siqa-*.json`

| Case | Result |
|------|--------|
| Session signed-in | **Pass** |
| a11y Feedback / Send message | **Pass** |
| LED blink | **Pass** — Verified live, `Authorization` on POST, ~7s, 200 |
| Draft adopt (`/` → `/p/…/edit`) | **Pass** — chat kept, verified bubble survived |
| Freeform send (no Feedback steal) | **Pass** |
| Freeform behavior change (serial print) | **Partial** — agent ran; `edit_firmware` hit **STARTER_ALREADY_PROVEN** mid freeform turn (model re-hydrated proven starter then was blocked from editing) |

### BUG-012 — STARTER_ALREADY_PROVEN blocks freeform edits in same turn after re-load
- **Severity:** High (signed-in freeform adaptation)
- **Status:** Open
- **Detail:** After LED verified, user asked for serial HELLO; agent hit `firmware → mcu: Full starter already proven… Do not edit` even though the user explicitly requested a change. Gate should allow edit when the user message asks for new behavior, or not re-load a proven starter on a modification brief.



---

## Multi-device catalog experiments (2026-07-29 / 30)

**Goal:** Push catalog chips/parts; ask Architect to build many different devices.

**Catalog inventory used:**
- Chips: esp32-c3, esp32-s3, classic esp32, rp2040, nrf52840, stm32f103, stm32l476
- Starter templates: led_blink, button_led, oled_i2c, serial_hello, traffic_light, ultrasonic, weather_bme280, doorbell_buzzer
- Labs: `esp32c3-oled-lab`, `rp2040-pico`, `nrf52840-proximity-lab`, `stm32f103-blinky`, `esp32s3-oled-lab`, `esp32`, `nokia5110-invaders-lab`
- Catalog size: ~13 chips, ~136 parts (bme280, ssd1306, buzzer, hc-sr04, leds, buttons, …)

### Product QA (signed-in) — all green

| Case | Result |
|------|--------|
| LED blink starter | **PASS** Verified live (auth) |
| Freeform serial change (BUG-012) | **PASS** no STARTER_ALREADY_PROVEN |
| Freeform blink 200ms | **PASS soft** settled, no crash/block |

Artifacts: `PRODUCT-QA-2026-07-29.json`, `pqa-*.json`

### Headless multi-device matrix — 11/12 agent ok, 3 verified live

| id | chip | device | result | verified |
|----|------|--------|--------|----------|
| c3-led-starter | esp32-c3 | led_blink | PASS | yes |
| c3-traffic-ff | esp32-c3 | traffic_light | PASS | yes (parts 4, assembly_set) |
| c3-weather-ff | esp32-c3 | weather_bme280 | PASS | yes |
| c3-ultrasonic-ff | esp32-c3 | ultrasonic | PASS soft | no — twin never proven |
| c3-doorbell-ff | esp32-c3 | doorbell_buzzer | PASS soft | no |
| pico-led-ff | rp2040 | led_blink | PASS soft | no — search only |
| pico-oled-ff | rp2040 | oled_i2c | PASS soft | no — search only |
| nrf-led-ff | nrf52840 | led_blink | PASS soft | no — search only |
| stm32-f103-ff | stm32f103 | led_blink | PASS soft | no — search only |
| s3-oled-ff | esp32-s3 | oled_i2c | PASS soft | no — search only |
| esp32-button-ff | esp32 | button_led | **FAIL** timeout | no agent POST |
| nokia-l476-ff | stm32l476 | led_blink | PASS soft | no — search only |

Artifacts: `MULTI-DEVICE-CATALOG-LATEST.md`, `MULTI-DEVICE-HEADLESS-*.json`, `mdq-hl-*.json/png`

### New findings

### BUG-013 — Ultrasonic HC-SR04 freeform builds but twin never proves
- **Severity:** Medium (device quality)
- **Status:** Open
- **Repro:** Freeform “HC-SR04 ultrasonic distance on ESP32-C3… prove”
- **Actual:** Agent calls `search_similar_labs` → `load_starter` → `assembly_set` → `run_and_verify` (multiple retries); chat: “the run didn’t reach a proven state”
- **Expected:** Verified live with `distance_cm` serial markers (starter `esp32-c3-ultrasonic` exists)
- **Artifact:** `mdq-hl-c3-ultrasonic-ff.json`

### BUG-014 — Cross-chip freeform often stops after search_similar_labs (no load/prove)
- **Severity:** High (product quality for non-C3 chips)
- **Status:** Open
- **Chips hit:** rp2040, nrf52840, stm32f103, esp32-s3, stm32l476 (Nokia lab)
- **Actual:** Stream finishes with only `search_similar_labs` ×2; no `load_starter` / `assembly_set` / `run_and_verify`; no “Verified live”
- **Expected:** Same retrieve-before-write path as C3 traffic/weather — load matching starter, assemble, prove
- **Note:** C3 traffic/weather succeed with assembly/prove; gap is model/tool follow-through off C3 OLED lab

### BUG-015 — Classic ESP32 board freeform can hang (no /v1/agent completion)
- **Severity:** Medium
- **Status:** Open
- **Repro:** `?board=esp32` + freeform button+LED
- **Actual:** Thinking spinner ~90s+, zero completed agent POSTs observed; timeout
- **Expected:** Agent stream completes with build or clear error
- **Artifact:** `mdq-hl-esp32-button-ff.json`

### Quality notes (not new bugs)
- ESP32-C3 is the strongest path: LED / traffic / weather freeforms reach **Verified live**
- Doorbell uses `assembly_set` but didn’t show Verified bubble in window
- Catalog starters exist for multi-MCU LED/OLED/button; agent should prefer `load_starter` over chat-only answers



---

## Reliability 4-fix coverage (merged)

Merged to main:
- #1098 generic reliable build loop
- #1099 unit coverage + live-canvas diagramChip residual

| Fix | What | Status |
|-----|------|--------|
| 1 | Unproven auto-load continues (no Finish NOW) | Unit + live nRF now calls run_and_verify×N |
| 2 | Structural prepareStep rails | Unit + live tool chains show search→prove |
| 3 | Prove on live canvas board | liveCanvasBoard() + diagramChip fix (#1099) |
| 4 | Step budgets prove-retry | STARTER_PROVE=10 FULL_BUILD=14 unit tests |

Live retest (headless, post-#1098 deploy, pre-#1099 full deploy may lag):
- C3 LED/traffic/weather: Verified live
- Pico/nRF/STM: agent continues prove (run_and_verify), not search-only
- Twin prove still fails on some non-C3 (infra/twin quality, not loop abandon)

