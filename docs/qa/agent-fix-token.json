### Result
{
  "via": "Clerk.session",
  "ms": 1,
  "hasToken": true,
  "tokenLen": 808
}
### Ran Playwright code
```js
await page.evaluate('async () => {\n    const t0 = Date.now();\n    try {\n      if (window.Clerk?.session?.getToken) {\n        const tok = await Promise.race([\n          window.Clerk.session.getToken(),\n          new Promise((_, rej) => setTimeout(() => rej(new Error(\'token-timeout-5s\')), 5000)),\n        ]);\n        return { via: \'Clerk.session\', ms: Date.now()-t0, hasToken: !!tok, tokenLen: tok ? String(tok).length : 0 };\n      }\n      return { via: \'none\', ms: Date.now()-t0, clerk: !!window.Clerk, session: !!window.Clerk?.session };\n    } catch (e) {\n      return { via: \'error\', ms: Date.now()-t0, error: String(e) };\n    }\n  }');
```