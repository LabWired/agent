### Result
{
  "hasTextarea": true,
  "valueLen": 44,
  "sendBtn": {
    "disabled": false,
    "aria": "Send feedback",
    "text": "Feedback"
  },
  "starters": [
    {
      "text": "LED blink",
      "disabled": false
    },
    {
      "text": "Button + LED",
      "disabled": false
    },
    {
      "text": "OLED hello",
      "disabled": false
    },
    {
      "text": "Serial hello",
      "disabled": false
    },
    {
      "text": "Prove it works",
      "disabled": false
    }
  ],
  "clickedLed": true,
  "sendBefore": {
    "disabled": false
  },
  "forceClickedSend": true
}
### Ran Playwright code
```js
await page.evaluate('() => {\n    const out = {};\n    const ta = document.querySelector(\'textarea[aria-label="Message the Architect"], textarea[placeholder*="Describe"]\');\n    out.hasTextarea = !!ta;\n    if (ta) {\n      const setter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, \'value\')?.set;\n      ta.focus();\n      setter?.call(ta, \'Print LABWIRED_FORCE_OK on UART every second\');\n      ta.dispatchEvent(new Event(\'input\', { bubbles: true }));\n      ta.dispatchEvent(new Event(\'change\', { bubbles: true }));\n      out.valueLen = ta.value.length;\n      out.sendBtn = (() => {\n        const buttons = [...document.querySelectorAll(\'button\')];\n        const send = buttons.find(b => (b.getAttribute(\'aria-label\')||b.textContent||\'\').includes(\'Send\') || b.textContent?.includes(\'↑\'));\n        if (!send) return null;\n        return { disabled: send.disabled, aria: send.getAttribute(\'aria-label\'), text: (send.textContent||\'\').trim() };\n      })();\n    }\n    const starters = [...document.querySelectorAll(\'button\')].filter(b => /LED blink|Serial hello|OLED hello|Prove it works|Button/i.test(b.textContent||\'\'));\n    out.starters = starters.map(b => ({ text: b.textContent.trim(), disabled: b.disabled }));\n    // click LED blink via DOM\n    const led = starters.find(b => /LED blink/i.test(b.textContent||\'\'));\n    if (led) { led.click(); out.clickedLed = true; }\n    // try enable send and click\n    if (ta) {\n      const buttons = [...document.querySelectorAll(\'button\')];\n      const send = buttons.find(b => (b.getAttribute(\'aria-label\')===\'Send\') || /^\\s*↑\\s*$/.test(b.textContent||\'\') || (b.textContent||\'\').includes(\'↑\'));\n      if (send) {\n        out.sendBefore = { disabled: send.disabled };\n        if (send.disabled) {\n          send.disabled = false;\n          send.removeAttribute(\'disabled\');\n        }\n        send.click();\n        out.forceClickedSend = true;\n      }\n    }\n    return out;\n  }');
```
### Page
- Page URL: https://app.labwired.com/?lab=esp32c3-oled-lab
- Page Title: LabWired Playground — The easy way to build hardware
- Console: 1 errors, 0 warnings
### Events
- New console entries: .playwright-mcp/console-2026-07-29T15-33-06-570Z.log#L1