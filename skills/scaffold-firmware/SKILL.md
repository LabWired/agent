---
name: scaffold-firmware
description: >-
  Alias → use skill pack `bringup` instead. Kept for older prompts/configs.
  scaffold-firmware is folded into bringup.
license: MIT
compatibility: opencode
metadata:
  gate: "alias"
  labwired: "true"
  alias_of: "bringup"
---

# Alias: scaffold-firmware → **bringup**

This skill was merged into the **`bringup`** pack.

**Load `bringup`** (or **`golden-path`** for the full loop).

See `skills/README.md`.
